export interface TaskClients {
    select: boolean;
    id: string;
    taskName: string;
    description: string;
    startTime: string;
    endTime: string;
    address: string;
    clientid: string;
}